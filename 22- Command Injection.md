# Layer 1
## يعني إيه Command Injection؟

خلينا الأول نفهم يعني إيه **Command**.

الـ Command هو الأمر اللي بتكتبه في الـ Terminal أو الـ CMD أو الـ Bash.

زي:

```bash
ls
```

يعرض الملفات.

أو:

```bash
whoami
```

يعرفك أنت شغال بأي يوزر.

أو:

```bash
pwd
```

يعرفك أنت واقف في أنهي فولدر.

---

## طيب يعني إيه Injection؟

كلمة Injection معناها "حقن".

يعني البرنامج كان ناوي ينفذ حاجة معينة...

وأنت حقنت فيه كود أو أمر زيادة يخليه يعمل حاجة تانية.

يعني المطور كان عايز ينفذ:

```
touch report.pdf
```

وأنت خليته ينفذ:

```
touch report.pdf
whoami
```

أو

```
touch report.pdf && whoami
```

فبقى نفذ الأمرين.

---

## أول فقرة

> A Command Injection vulnerability is among the most critical types of vulnerabilities.

يعني:

ثغرة الـ Command Injection تعتبر من أخطر الثغرات الموجودة.

ليه؟

لأنها بتخليك تنفذ أوامر على السيرفر نفسه.

---

## يعني إيه السيرفر؟

أي موقع شغال على جهاز.

مثلاً

```
facebook.com
```

وراها جهاز Linux.

الجهاز ده هو اللي بيستقبل الريكوستات.

لو قدرت تنفذ أوامر عليه...

يبقى أنت بقيت تقريباً بتتحكم فيه.

---

## الجملة دي

> It allows us to execute system commands directly on the back-end hosting server

يعني:

تقدر تشغل أوامر Linux أو Windows مباشرة على السيرفر.

زي

```
ls
pwd
cat
whoami
id
hostname
```

أو على Windows

```
dir
whoami
ipconfig
```

---

## الجملة دي

> which could lead to compromising the entire network

يعني:

لو السيرفر ده موجود جوه شركة...

ممكن بعد ما تدخل عليه تبدأ تتحرك لباقي الأجهزة.

وده اسمه

```
Lateral Movement
```

يعني بدل ما تسيطر على جهاز واحد...

تسيطر على الشبكة كلها.

---

## الجملة دي

> If a web application uses user-controlled input

يعني

لو الموقع بياخد Input منك.

مثلاً

```
search=test
```

أو

```
filename=abc
```

أو

```
ip=8.8.8.8
```

كل دي User Input.

---

## الجملة دي

> to execute a system command

يعني المطور استخدم الـ Input ده جوه أمر Linux.

مثلاً

هو كتب

```php
system("ping ".$ip);
```

يعني هيعمل

```
ping 8.8.8.8
```

---

## المشكلة هنا

لو أنا بعت

```
8.8.8.8 && whoami
```

الأمر هيبقى

```
ping 8.8.8.8 && whoami
```

السيرفر هينفذ الاتنين.

---

وده اسمه

Command Injection.

---

## What are Injections

يعني

إيه هي الـ Injection بشكل عام؟

---

## الجملة

> Injection vulnerabilities are considered the number 3 risk in OWASP Top 10

يعني

منظمة OWASP حطت الـ Injection ضمن أخطر ثغرات الويب.

ليه؟

لأنها:

- منتشرة جداً.
    
- تأثيرها كبير جداً.
    
- ممكن تؤدي لاختراق كامل.
    

---

## الجملة

> Injection occurs when user-controlled input is misinterpreted

دي أهم جملة.

يعني

البرنامج فهم مدخلاتك بطريقة غلط.

بدل ما يفهمها إنها بيانات...

فهمها إنها جزء من الكود.

---

مثال

المفروض المستخدم يكتب

```
Mohab
```

لكن كتب

```
Mohab && whoami
```

البرنامج افتكر إن

```
&& whoami
```

جزء من الأمر.

---

وده سبب المشكلة.

---

## أنواع الـ Injection

---

## أول نوع

### OS Command Injection

يعني

بتحقن أوامر نظام تشغيل.

مثلاً

```
ls
```

أو

```
cat
```

أو

```
whoami
```

---

## ثاني نوع

### Code Injection

يعني

بدل ما تنفذ أوامر Linux...

بتنفذ كود من نفس لغة البرمجة.

مثلاً PHP.

بدل ما يقرأ

```php
hello
```

تخليه ينفذ

```php
phpinfo();
```

---

## ثالث نوع

### SQL Injection

ودي أشهر واحدة.

بدل ما الاستعلام يبقى

```sql
SELECT * FROM users
WHERE username='mohab'
```

تخليه يبقى

```sql
SELECT * FROM users
WHERE username='' OR 1=1
```

فترجع كل المستخدمين.

---

## رابع نوع

### XSS

يعني بدل ما الصفحة تعرض كلامك...

تعرض JavaScript.

مثلاً

```html
<script>alert(1)</script>
```

بدل ما يظهر كنص...

يتنفذ.

---

## باقي أنواع الـ Injection

الكتاب بيقول فيه أنواع كتير.

زي

LDAP

NoSQL

XPath

HTTP Header

IMAP

ORM

كل واحدة منهم بتحقن في حاجة مختلفة.

لكن الفكرة واحدة.

---

## الفكرة العامة لكل أنواع الـ Injection

المستخدم دخل بيانات.

↓

المطور دخل البيانات جوه Query.

↓

من غير حماية.

↓

المهاجم هرب من مكان البيانات.

↓

وبقى بيتحكم في الـ Query.

---

وده ملخص كل أنواع الـ Injection.

---

## الجملة

> without being properly sanitized

دي كلمة مهمة جداً.

Sanitize يعني

تنضف المدخلات.

مثلاً

لو المستخدم كتب

```
;
```

تمسحه.

أو

```
&
```

تمسحه.

أو

```
|
```

تمسحه.

أو

```
`
```

تمسحه.

---

أو تعمل Escape.

---

## يعني إيه Escape؟

بدل ما

```
&
```

يتفهم إنه Operator.

يتعامل كنص عادي.

---

مثلاً

```
hello && whoami
```

يتحول إلى

```
hello\ &&\ whoami
```

أو

```
"hello && whoami"
```

حسب اللغة.

---

## OS Command Injection

الكتاب بيقول

علشان تبقى فيه Command Injection

لازم الـ Input بتاعك يوصل في الآخر لأمر بيتنفذ على الـ OS.

---

يعني

أنت كتبت

```
Mohab
```

↓

راح لـ PHP

↓

PHP راحت لـ Linux

↓

Linux نفذ أمر.

---

لو الـ Input عمره ما وصل للـ Shell...

يبقى مفيش Command Injection.

---
## مثال PHP

![[Pasted image 20260705215903.png]]


```php
if (isset($_GET['filename'])) {
```

يعني

لو فيه Parameter اسمه

```
filename
```

---

مثلاً

```
site.com?filename=test
```

---

بعدها

```php
system(...)
```

دي Function في PHP.

وظيفتها

تشغل أمر Linux.

---

بعدها

```php
system("touch /tmp/" . $_GET['filename'] . ".pdf");
```

نفكها.

لو المستخدم كتب

```
report
```

هيبقى

```
touch /tmp/report.pdf
```

---


## فين المشكلة؟

المشكلة إن

```php
$_GET['filename']
```

دخلت جوه الأمر مباشرة.

من غير حماية.

---

يعني لو أنا بعت

```
report;whoami
```

الأمر هيبقى

```
touch /tmp/report;whoami.pdf
```

أو باستخدام فواصل أو معاملات أخرى حسب الـ Shell والسياق، وده قد يسمح بتنفيذ أوامر إضافية لو التطبيق قابل للاستغلال.

المشكلة الأساسية إن المستخدم قدر يأثر على الأمر اللي هيتنفذ.

---

## NodeJS Example
![[Pasted image 20260705220027.png]]


```javascript
child_process.exec(...)
```

دي Function في NodeJS.

بتعمل نفس وظيفة

```php
system()
```

---

يعني تشغل أمر Linux.

---

الكود

```javascript
child_process.exec(`touch /tmp/${req.query.filename}.txt`);
```

لو المستخدم كتب

```
abc
```

يبقى

```
touch /tmp/abc.txt
```

---

ولو المطور ما عملش حماية للمدخلات، ممكن المستخدم يحاول يضيف معاملات أو أوامر إضافية، فيتحول الأمر المنفذ إلى شيء مختلف عن اللي المطور كان يقصده.

---

## آخر نقطة

الكتاب بيقول

الموضوع مش خاص بـ PHP.

ولا NodeJS.

ولا Python.

ولا Java.

ولا Ruby.

ولا Go.

أي لغة فيها Function تشغل أوامر نظام التشغيل...

لو المطور دخل فيها Input المستخدم من غير حماية...

يبقى معرضة لـ Command Injection.

---

## الخلاصة

الفكرة كلها في سطر واحد:

- الموقع بياخد Input من المستخدم.
    
- المطور يحطه داخل أمر نظام التشغيل.
    
- لو المدخلات مش متفلترة أو متأمنة، المهاجم يقدر يغيّر شكل الأمر.
    
- النتيجة ممكن تكون تنفيذ أوامر غير مقصودة على السيرفر، ودي من أخطر الثغرات لأنها قد تؤدي للسيطرة على النظام بالكامل.
    

وده الأساس اللي هتبني عليه كل طرق اكتشاف واستغلال ثغرات **OS Command Injection** بعد كده.


-------
# Layer 2

## يعني إيه Detection؟

أنت دلوقتي **لسه مش متأكد إن فيه Command Injection ولا لأ**.

يعني قدامك موقع، ومفيش Source Code، ومتعرفش هو مكتوب بإيه.

كل اللي عندك إنك بتتعامل مع الموقع كمستخدم عادي.

فإزاي تعرف إنه Vulnerable؟

---

## الفكرة الأساسية

أنت بتحاول تخلي الموقع يعمل حاجة **غير اللي المطور كان قاصدها**.

لو نجحت...

يبقى غالبًا فيه Command Injection.

بمعنى:

المطور كان عايز ينفذ أمر واحد.

أنت خليته ينفذ أمرين.

لو حصل كده...

خلاص، اكتشفت الثغرة.

---

## يعني إيه "The process of detecting is the same as exploiting"؟

دي من أهم الجمل.

في ثغرات كتير الاكتشاف بيكون مختلف عن الاستغلال.

لكن هنا...

**مجرد إنك قدرت تنفذ أمر، يبقى أنت بالفعل استغليت الثغرة.**

يعني مفيش مرحلة:

- Detect
    
- بعدين Exploit
    

لا.

أول أمر يشتغل...

يبقى الثغرة Confirmed.

---

## ليه بيقول Basic Command Injection؟

الكتاب بيفرق بين نوعين.

## الأول

Basic

وده اللي هنتعلمه دلوقتي.

بيبقى فيه حاجة زي

```
IP Address
```

أو

```
Filename
```

أو

```
Hostname
```

والمطور واخد القيمة دي وحاططها مباشرة في أمر Linux.

يعني الطريق واضح.

---

## النوع التاني

Advanced

وده أصعب بكتير.

ممكن تكون البيانات بتعدي على عشر Functions.

أو يحصل Validation.

أو Encoding.

أو Filtering.

أو تتخزن الأول في Database.

أو تتبعت لسيرفر تاني.

هنا الموضوع بيحتاج:

- Fuzzing
    
- Source Code Review
    
- Logic Analysis
    

وده مش موضوعنا دلوقتي.

---

## المثال اللي الكتاب شرحه

قدامك صفحة اسمها

```
Host Checker
```

يعني برنامج بسيط بيقولك:

> اكتبلي IP وأنا هقولك الجهاز ده شغال ولا لأ.

---

مثلاً تكتب

```
127.0.0.1
```

---

## ليه اختاروا 127.0.0.1 ؟

دي حاجة لازم تبقى فاهمها.

127.0.0.1

ده اسمه

```
Loopback Address
```

أو

```
localhost
```

يعني الجهاز نفسه.

كأن السيرفر بيكلم نفسه.

---

فلما تضغط Check

يرجعلك

```
Host is alive
```

أو

```
1 packet transmitted
1 received
```

---

## هنا بقى بدأ شغل الهاكر

هو بيفكر كده:

الموقع عرف منين إن الجهاز شغال؟

أكيد استخدم أمر.

إيه أشهر أمر بيختبر إن جهاز شغال؟

أكيد

```
ping
```

---

يبقى بنسبة كبيرة الكود عامل كده

```
ping -c 1 127.0.0.1
```

---

## ليه -c 1 ؟

في Linux

```
ping
```

بيفضل يشتغل للأبد.

لكن

```
-c 1
```

يعني ابعت Packet واحدة بس.

علشان يجيب النتيجة بسرعة.

---

يبقى الهاكر استنتج بدون Source Code إن غالبًا السيرفر بينفذ

```
ping -c 1 <User Input>
```

---

## طب هو عرف إزاي؟

دي نقطة مهمة جدًا.

إحنا في البنتست بنستنتج من الـ Response.

مش لازم أشوف الكود.

أنا بشوف النتيجة.

وبعرف إيه الأمر اللي غالبًا اتنفذ.

---

مثلاً لو لقيت

```
PING google.com
64 bytes
ttl
icmp_seq
```

يبقى ده Ping.

---

لو لقيت

```
Traceroute
```

يبقى Traceroute.

---

لو لقيت

```
nslookup
```

يبقى DNS Lookup.

---

يعني الـ Output بيكشفلك الأمر.

---

## بعد كده بيبدأ الاختبار

دلوقتي أنت عرفت إن الموقع غالبًا بينفذ

```
ping -c 1 YOUR_INPUT
```

السؤال

هل الـ YOUR_INPUT متأمن؟

ولا بيتحط زي ما هو؟

---

وده اللي هنختبره.

---

## إزاي نختبر؟

هنحاول نضيف أمر تاني.

بدل ما يبقى

```
127.0.0.1
```

نخليه

```
127.0.0.1 [Operator] Command
```

يعني

```
127.0.0.1
```

عامل (Operator)

أمر جديد.

---

## يعني إيه Operator؟

دي من أهم الحاجات في Command Injection.

الـ Operator هو رمز الـ Shell بيفهمه.

زي

```
;
```

أو

```
&&
```

أو

```
||
```

أو

```
|
```

كل واحدة ليها معنى.

خلينا نشرحهم واحدة واحدة.
![[Pasted image 20260705220930.png]]

---

## 1) Semicolon ;

دي أسهل واحدة.

معناها ببساطة

> خلص الأمر الأول... وبعده نفذ الأمر اللي بعده.

يعني

```
Command1 ;
Command2
```

السيرفر هيعمل

```
نفذ الأول

بعده نفذ التاني
```

بغض النظر الأول نجح ولا فشل.

---

## 2) New Line

ودي ناس كتير بتنساها.

الـ Shell أصلاً بيعتبر

Enter

معناه

ابدأ أمر جديد.

يعني

```
ping
```

Enter

```
whoami
```

دي بالنسبة للـ Shell أمرين.

---

فلو قدرت تدخل

```
New Line
```

جوه الـ Input...

هيعتبر إن فيه أمر جديد.

---

## 3) علامة &

في Linux

```
&
```

بتشغل الأمر في الخلفية.

يعني

```
Command1 &
Command2
```

الاتنين يشتغلوا.

---

الكتاب كاتب ملاحظة:

غالبًا هتشوف ناتج الأمر التاني الأول.

ليه؟

لأن الأول شغال Background.

---

## 4) Pipe |

دي مختلفة شوية.

الـ Pipe الطبيعي وظيفته إنه يبعت **ناتج الأمر الأول كمدخل للأمر الثاني**.

مثلاً:

```bash
cat file.txt | grep admin
```

يعني:

- `cat` يقرأ الملف.
    
- الناتج يروح لـ `grep`.
    
- `grep` يدور على كلمة `admin`.
    

لكن في بعض تطبيقات الويب، لو قدرت تحقن `|`، ممكن تلاحظ إن **النتيجة اللي بتظهر لك هي نتيجة الأمر اللي بعد الـ Pipe فقط**، وده بيساعدك تعرف إن الحقن نجح.

---

## 5) &&

دي معناها

نفذ الأمر الثاني

**لو الأول نجح فقط.**

مثلاً

```
ping google.com && whoami
```

لو الـ Ping نجح

هيشغل

```
whoami
```

---

ولو الـ Ping فشل؟

ولا حاجة.

---

## 6) ||

دي العكس.

نفذ الأمر الثاني

لو الأول فشل.

يعني

```
ping InvalidIP || whoami
```

الـ Ping هيفشل.

إذن

هيشغل

```
whoami
```

---

## 7) Backticks ``

دي خاصة بـ Linux.

لما تكتب أمر بين علامتي ``

الـ Shell ينفذه الأول.

ودي اسمها **Command Substitution**.

يعني بدل ما تعتبر اللي جوه نص، تعتبره ناتج أمر.

---

## 8) $()

دي نفس فكرة الـ Backticks.

برضو اسمها

Command Substitution.

ودي النسخة الأحدث والأكثر استخدامًا في Bash.

---

## ليه كل الـ Operators دي مهمة؟

لأنك مش هتلاقي كل المواقع بتقبل نفس الحاجة.

ممكن موقع يمنع

```
;
```

لكن يقبل

```
&&
```

وموقع تاني يمنع

```
&&
```

لكن يقبل

```
|
```

وموقع تالت يمنع كل دول

لكن ينسى

```
New Line
```

عشان كده البنتستر بيجرب أكتر من Operator، مش واحد بس.

---

## ليه فيه URL Encoded جنب كل Operator؟

لأن أحيانًا المتصفح أو التطبيق بيمنع إرسال الرموز دي بشكل مباشر.

فبدل ما تبعت:

```
;
```

بتتبعت بالشكل المشفر الخاص بالـ URL.

يعني كل رمز ليه تمثيل Hexadecimal يبدأ بـ `%`.

وده بيساعد الرمز يوصل للسيرفر بدون ما يتغير أو يتمنع في بعض الحالات.

---

## آخر نقطة في الدرس

الكتاب بيقول:

سواء الموقع مكتوب بـ:

- PHP
    
- NodeJS
    
- ASP.NET
    
- Python
    
- Java
    

**مش فارقة.**

ليه؟

لأن اللي في الآخر بينفذ الأمر هو **نظام التشغيل (Shell)**، مش لغة البرمجة نفسها.

لغة البرمجة مجرد وسيط بيقول للـ OS:

> "نفذ الأمر ده."

فطالما المطور مرر مدخلات المستخدم للـ Shell من غير حماية، فطريقة اكتشاف واستغلال الـ Command Injection هتكون متشابهة جدًا بغض النظر عن لغة البرمجة المستخدمة.

**وده السبب إن فهمك للـ Shell Operators أهم بكتير من حفظ دوال PHP أو NodeJS.** لما تفهم إزاي الـ Shell بيفسر الأوامر، هتقدر تتعامل مع Command Injection على أغلب التقنيات المختلفة.

-----

# Layer 3
## إحنا وصلنا لفين؟

في الدرس اللي فات عرفنا إن الموقع فيه صفحة اسمها:

**Host Checker**

بتدخل IP.

والموقع بيروح يعمل عليه Ping.

يعني غالبًا الكود في الخلفية عامل حاجة شبه:

```bash
ping -c 1 USER_INPUT
```

وده مجرد استنتاج من شكل الـ Output.

لكن لسه معندناش دليل إن فيه Command Injection.

دلوقتي هنحاول نثبت ده.

---

## أول حاجة بيعملها البنتستر

هو بيسأل نفسه:

> "لو أنا ضفت أمر تاني... هل السيرفر هينفذه؟"

يعني بدل ما يدخل

```text
127.0.0.1
```

يدخل

```text
127.0.0.1; whoami
```

---

## ليه اختار whoami؟

دي نقطة مهمة جدًا.

في أول اختبار، البنتستر **مش بيختار أوامر خطيرة**.

مش هيبدأ بـ:

- حذف ملفات
    
- فتح Reverse Shell
    
- تنزيل Malware
    

لأ.

هو عايز أمر:

- سريع
    
- آمن
    
- ليه Output واضح
    
- يثبت إن الأمر اتنفذ
    

وعشان كده `whoami` من أفضل أوامر الاختبار.

لأنه بيرجع اسم اليوزر اللي شغال بيه السيرفر.

مثلاً:

```bash
www-data
```

أو

```bash
apache
```

أو

```bash
root
```

---

## إيه اللي هيحصل؟

لو الكود فعلاً:

```bash
ping -c 1 USER_INPUT
```

وأنت دخلت

```text
127.0.0.1; whoami
```

يبقى السيرفر هيشوف:

```bash
ping -c 1 127.0.0.1; whoami
```

والـ Shell هيفهمها كده:

1. اعمل Ping.
    
2. بعد ما تخلص، شغل whoami.
    

---

## ليه الكتاب شغل الأمر على Linux الأول؟

قبل ما يجرب على الموقع، جربه على جهازه.

يعني كتب:

```bash
ping -c 1 127.0.0.1; whoami
```

ولقى الناتج:

```
PING ...
...
21y4d
```

يعني:

- الـ Ping اشتغل.
    
- وبعده `whoami` اشتغل.
    

وده بيأكد إن الـ Payload نفسه صحيح.

يعني لو فشل بعد كده على الموقع، يبقى المشكلة من الموقع، مش من الـ Payload.

ودي عادة كويسة جدًا في البنتست.

---

## بعد كده راح للموقع

دخل:

```text
127.0.0.1; whoami
```

لكن...

الموقع رفض.

---

## هل كده الثغرة مش موجودة؟

ناس كتير هنا بتقف.

وتقول:

> خلاص الموقع منعني.

لكن البنتستر المحترف بيفكر بطريقة تانية.

هو بيسأل:

**مين اللي رفض؟**

وده سؤال مهم جدًا.

---

## عندنا نوعين من الـ Validation

## الأول: Front-end Validation

وده اللي بيحصل في المتصفح.

يعني JavaScript.

قبل ما الريكوست يطلع أصلاً.

---

مثلاً

أنت بتكتب:

```text
abc
```

وهو يقولك:

```
Please enter valid IP
```

بدون ما يبعت أي Request.
![[Pasted image 20260705221542.jpg]]

---

## الثاني: Back-end Validation

وده اللي بيحصل على السيرفر.

يعني حتى لو عدلت الريكوست بنفسك...

السيرفر يراجعها.

ولو فيها مشكلة...

يرفضها.

وده الصح.

---

## ليه المطور يعمل Validation في الـ Front-end؟

علشان يحسن تجربة المستخدم.

مثلاً:

بدل ما تبعت Email غلط.

يقولك بسرعة:

```
Invalid Email
```

---

أو

بدل ما تبعت IP غلط.

يقولك:

```
Invalid IP
```

---

وده كويس.

لكن...

**مش أمان.**

---

## ليه مش أمان؟

لأن الهاكر مش مضطر يستخدم الصفحة.

هو يقدر يبعت HTTP Request بنفسه.

## دخل على Burp Suite

ليه؟

علشان Burp بيقف بين:

المتصفح

↓

السيرفر

---

## بعد كده عدل الريكوست

كان:

```
ip=127.0.0.1
```

خلاه:

```
ip=127.0.0.1; whoami
```
![[Pasted image 20260705221704.jpg]]

---

## ليه عمل URL Encoding؟

دي نقطة ناس كتير بتتلخبط فيها.

علامة

```
;
```

ليها معنى في الـ URL.

فممكن المتصفح يغيرها.

أو السيرفر يفسرها غلط.

---

فعشان كده Burp بيحولها لـ:

```
%3B
```

لكن لما توصل للسيرفر...

ترجع:

```
;
```

عادي.

---

## بعد كده ضغط Send

والسيرفر رد.

---

إيه اللي لقاه؟

لقي:

```
PING ...
```

وتحتها:

```
www-data
```

أو اسم المستخدم اللي السيرفر شغال بيه.

---

## معنى ده إيه؟

وده أهم سؤال.

يعني:

السيرفر نفذ:

```
ping
```

وبعدين نفذ:

```
whoami
```

---

يبقى...

مين اللي شغل whoami؟

المطور؟

لا.

أنا.

---

يبقى أنا قدرت أضيف أمر جديد.

---

وده اسمه:

**OS Command Injection.**

---

## ليه دي تعتبر Confirmed؟

لأننا مش بنخمن.

إحنا عندنا دليل.

الدليل هو إن Output الأمر اللي إحنا كتبناه ظهر في الـ Response.

يعني:

أنا كتبت:

```
whoami
```

والسيرفر رجع:

```
www-data
```

إذن السيرفر نفذ الأمر فعلًا.

وده بيسموه:

**Command Execution Evidence**.

---

## الدرس اللي لازم يفضل في دماغك

في البنتست فيه قاعدة مشهورة جدًا:

> **Never trust client-side validation.**

يعني:

**عمر ما تعتبر إن الموقع آمن لمجرد إن الصفحة منعتك تكتب Payload.**

طالما المنع حصل في الـ Front-end، فأنت غالبًا تقدر تتجاوزه باستخدام أدوات زي **Burp Suite**، لأن اللي بيحمي فعلًا هو **التحقق على السيرفر (Back-end Validation)**، مش اللي بيحصل في المتصفح.

الفالديشن : 
`<input type="text" name="ip" placeholder="127.0.0.1" pattern="^((\d{1,2}|1\d\d|2[0-4]\d|25[0-5])\.){3}(\d{1,2}|1\d\d|2[0-4]\d|25[0-5])$">`


# Layer 4

---

## الأول نفتكر حاجة

إحنا افترضنا إن الكود في السيرفر عامل كده:

```bash
ping -c 1 USER_INPUT
```

يعني أي حاجة هتكتبها هتتحط مكان `USER_INPUT`.

---

## أول Operator: &&

خلينا الأول نفهم هو بيعمل إيه في Linux.

لو كتبت:

```bash
echo Hello && whoami
```

الـ Shell هيقول:

1. نفذ `echo Hello`.
    
2. لو نجح، نفذ `whoami`.
    

يبقى `&&` معناها:

> **نفذ الأمر الثاني بشرط إن الأول ينجح.**

---

## نجرب على مثالنا

إحنا كتبنا:

```text
127.0.0.1 && whoami
```

السيرفر هيكون الأمر عنده:

```bash
ping -c 1 127.0.0.1 && whoami
```

إيه اللي هيحصل؟

### الخطوة الأولى

```bash
ping -c 1 127.0.0.1
```

نجح.

الـ `ping` رجع إن الجهاز موجود.

في Linux أي أمر بينجح بيرجع **Exit Code = 0**.

ودي نقطة مهمة جدًا.


![[Pasted image 20260705222038.jpg]]

---

### يعني إيه Exit Code؟

كل أمر في Linux لما يخلص بيرجع رقم.

الرقم ده بيقول للـ Shell:

"أنا نجحت" أو "أنا فشلت".

أشهر القيم هي:

- `0` = Success ✅
    
- أي رقم غير 0 (زي 1 أو 2 أو 127...) = Failure ❌
    

مثال:

```bash
ls
echo $?
```

هتلاقي:

```text
0
```

يعني `ls` اشتغل.

---

ولو كتبت:

```bash
ls file_that_does_not_exist
echo $?
```

هتلاقي:

```text
2
```

يعني الأمر فشل.

---

## نرجع لـ &&

بما إن الـ `ping` رجع Exit Code = 0

فالـ Shell يقول:

> تمام... نفذ اللي بعد `&&`.

فيشغل:

```bash
whoami
```

وده اللي حصل فعلًا.

---

## الكتاب سألك سؤال

> لو كتبنا `&& whoami` من غير IP، هيشتغل؟

الإجابة: **لا**.

ليه؟

لأن الأمر هيبقى:

```bash
ping -c 1 && whoami
```

لاحظ إن `ping` محتاج Destination.

يعني محتاج IP.

فلما يلاقي مفيش IP هيقول:

```text
Destination address required
```

يبقى الأمر الأول فشل.

ولأنه أصلًا أمر غير مكتمل، فمش ده السيناريو الطبيعي اللي عايزينه مع `&&`.

---

## بعد كده جربوه في Burp

بدل:

```text
ip=127.0.0.1
```

كتبوا:

```text
ip=127.0.0.1 && whoami
```

وعملوا URL Encoding.

وبعتوه.

السيرفر نفذ:

```bash
ping -c 1 127.0.0.1 && whoami
```

فرجع:

- نتيجة الـ Ping.
    
- ونتيجة `whoami`.
    

يبقى الـ Injection نجحت.

---

## طيب نروح لـ ||

ودي ناس كتير بتتلخبط فيها.

`||` معناها:

> **نفذ الأمر الثاني لو الأول فشل.**

ودي عكس `&&`.

---

## مثال بسيط

```bash
mkdir test || echo failed
```

لو الفولدر اتعمل...

يبقى `echo failed` مش هيشتغل.

---

لكن لو كتبت:

```bash
mkdir /root/test || echo failed
```

وأنت معندكش صلاحية...

يبقى `mkdir` هيفشل.

فالـ Shell يشغل:

```bash
echo failed
```

---

## نطبقها على مثالنا

لو كتبنا:

```text
127.0.0.1 || whoami
```

الأمر هيبقى:

```bash
ping -c 1 127.0.0.1 || whoami
```

---

إيه اللي هيحصل؟

الـ Ping نجح.

يبقى Exit Code = 0.

الـ Shell يقول:

> خلاص... مش محتاج أشغل `whoami`.

لأن `||` بيشتغل **فقط لو الأول فشل**.

---

عشان كده الناتج كان:

نتيجة الـ Ping فقط.

مفيش `whoami`.

---

## طب إزاي نخلي || يشتغل؟

لازم نخلي الأمر الأول يفشل.

إزاي؟

بإننا نكسره.

بدل ما نكتب:

```text
127.0.0.1 || whoami
```

كتبوا:

```text
|| whoami
```
![[Pasted image 20260705222221.jpg]]

---

يبقى الأمر بقى:

```bash
ping -c 1 || whoami
```

---

لاحظ المشكلة.

`ping` مفيش بعده IP.

فهيقول:

```text
Destination address required
```

يعني فشل.

Exit Code ≠ 0.

---

فالـ Shell يقول:

> بما إن الأول فشل...

نفذ الثاني.

فيشغل:

```bash
whoami
```

---

وده اللي حصل.

---

## ليه الناتج بقى أنضف؟

في المثال الأول مع `;`

كان بيرجع:

```
نتيجة Ping

ثم

نتيجة whoami
```

---

لكن هنا

الـ Ping فشل.

فمش هيطلع Output كبير.

وهتشوف غالبًا:

```
www-data
```

بس.

وده بيخلي قراءة النتيجة أسهل.

---

## طيب ليه نهتم بكل Operator؟

لأنك في الواقع مش هتلاقي كل السيرفرات بتتصرف بنفس الطريقة.

مثال:

سيرفر يمنع:

```text
;
```

لكن يسمح:

```text
&&
```

---

سيرفر تاني يمنع:

```text
&&
```

لكن يسمح:

```text
|
```

---

سيرفر تالت يمنع الاتنين.

لكن ينسى:

```text
||
```

---

عشان كده البنتستر المحترف بيجرب أكتر من Operator.

---

$# الجدول الأخير

الكتاب بيقول فكرة مهمة جدًا.

كل نوع Injection ليه رموز بيستخدمها المهاجم.

يعني مثلاً:

### SQL Injection

أشهر الرموز:

```sql
'
;
--
/**/
```

لأنها بتأثر على طريقة كتابة الـ SQL Query.

---

### Command Injection

أشهرها:

```text
;
&&
|
```

لأنها Operators في الـ Shell.

---

### Directory Traversal

بتستخدم:

```text
../
```

ليه؟

علشان تطلع من الفولدر الحالي.

مثلاً:

```text
../../etc/passwd
```

---

### Header Injection

بتستخدم:

```text
\r\n
```

علشان تضيف Header جديد.

---

كل نوع من الـ Injection بيستغل **الرموز اللي اللغة أو البروتوكول اللي بيتعامل معاه بيفهمها**.

---

#$ آخر نقطة في الدرس

الكتاب بيقول:

إحنا لحد دلوقتي بنتعامل مع **Direct Command Injection**.

يعني:

```
Input
    ↓
System Command
    ↓
Output بيرجعلك
```

وده أسهل نوع.

لكن فيه أنواع أصعب هتدرسها بعدين، زي:

- **Blind Command Injection**: الأمر بيتنفذ لكن مفيش Output ظاهر، فتضطر تستنتج نجاحه بطرق غير مباشرة (زي الوقت أو الـ DNS أو الـ HTTP requests).
    
- **Indirect Command Injection**: مدخلاتك متتنفذش فورًا، لكنها توصل في مرحلة تانية لجزء بينفذ أوامر.
    

فابدأ بإتقان الـ **Direct Command Injection** الأول، لأنه الأساس اللي هتفهم منه كل الأنواع المتقدمة بعد كده.


# Layer 5

---

## إحنا وصلنا لفين؟

في الدرس اللي فات اكتشفنا إن الموقع Vulnerable.

وكنا بنبعت:

```text
127.0.0.1; whoami
```

وكان بيرجع:

```text
PING ...
www-data
```

يعني خلاص عرفنا إن فيه Command Injection.

---

## لكن...

المطور شاف المشكلة.

وقال:

> "أنا همنع أي حد يحاول يعمل Injection."

فبدأ يضيف حماية.

السؤال بقى:

**نوع الحماية دي إيه؟**

---

## أول نوع حماية

الكتاب بيتكلم عن حاجة اسمها

## Blacklist

يعني إيه Blacklist؟

تخيل إن عندك بواب على باب الشركة.

معاه ورقة مكتوب فيها:

```
;
&
|
whoami
cat
ls
```

وأي حد يدخل ومعاه واحدة من الحاجات دي...

يمنعه.

دي اسمها Blacklist.

يعني

> قائمة بالحاجات الممنوعة.

---

## وفيه حاجة اسمها Whitelist

ودي العكس.

بدل ما أقولك الممنوع.

أقولك:

المسموح هو بس:

- أرقام
    
- نقطة
    

أي حاجة غير كده...

ممنوعة.

ودي أقوى بكتير.

---

## الكتاب بيقول

المطورين ساعات يعملوا Blacklist.

مثلاً

لو لقوا

```text
;
```

يرفضوا.

أو

```text
&
```

يرفضوا.

أو

```text
|
```

يرفضوا.

---

## وفيه طبقة حماية أكبر

اسمها

## WAF

يعني

Web Application Firewall

---

خلينا نفهمه بطريقة بسيطة.

تخيل إن عندك:

```
أنت

↓

WAF

↓

الموقع
```

يعني قبل ما الريكوست يوصل للموقع...

يعدي على الحارس.

---

الحارس ده يبص.

لو لقى:

```text
<script>
```

يقول:

لا.

---

لقى:

```text
UNION SELECT
```

لا.

---

لقى:

```text
whoami
```

لا.

---

يبقى الريكوست أصلاً **مش هيوصل للموقع**.

---

## طب إيه الفرق بين الـ Blacklist والـ WAF؟

الـ Blacklist غالبًا بيبقى جوه الكود.

مثلاً في PHP.

أما الـ WAF فغالبًا بيبقى جهاز أو Software واقف قدام الموقع كله.

يعني بيحمي كل الـ Requests.

---

## بعد كده الكتاب دخل على الموقع

وجرب الـ Payload القديم

```text
127.0.0.1; whoami
```

لكن المرة دي...

ظهرله

```text
Invalid input
```
![[Pasted image 20260705223219.jpg]]

---

## السؤال هنا

هل ده معناه إن الثغرة راحت؟

الإجابة:

**مش بالضرورة.**

وده فرق مهم جدًا.

---

الثغرة ممكن تكون لسه موجودة.

لكن فيه فلتر منع الـ Payload.

يعني المشكلة بقت:

أنا عارف الثغرة موجودة.

لكن مش عارف أوصلها.

---

## طيب إزاي أعرف مين اللي منعني؟

الكتاب قال حاجة ذكية جدًا.

بص على شكل الرسالة.

---

## الحالة الأولى

لو ظهرلك

```text
Invalid Input
```

جوه نفس الصفحة.

يبقى غالبًا

الكود نفسه (PHP)

هو اللي رفض.

---

## الحالة الثانية

لو فجأة لقيت صفحة جديدة خالص.

مكتوب فيها مثلاً:

```
Access Denied

Blocked Request

Ray ID

Cloudflare
```

أو

```
Forbidden

Your request has been blocked
```

يبقى غالبًا ده WAF.

مش الموقع.
![[Pasted image 20260705223307.png]]

---

## بعد كده بدأ يفكر

أنا بعت:

```text
127.0.0.1; whoami
```

السؤال

الموقع رفض بسبب إيه؟

---

خلينا نفك الـ Payload.

هو فيه:

```
127.0.0.1
```

ودي أكيد سليمة.

---

بعدها

```
;
```

---

بعدها

Space

---

بعدها

```
whoami
```

---

يبقى فيه احتمالين.

---

## الاحتمال الأول

إن

```text
;
```

هو الممنوع.

---

## الاحتمال الثاني

إن

```text
whoami
```

هو الممنوع.

---

## الاحتمال الثالث

الاتنين.

---

## هنا بقى البنتستر بيشتغل بطريقة علمية

مش يقعد يخمن.

لا.

يبدأ يشيل حاجة حاجة.

وده اسمه:

Isolation

يعني بعزل كل عنصر لوحده.

---

بدل ما أجرب Payload كبير.

أجرب أصغر Payload ممكن.

---

مثلاً

بدل

```text
127.0.0.1; whoami
```

أخليها

```text
127.0.0.1;
```

بس.

---

ليه؟

علشان أعرف.

هل المشكلة في

```
;
```

ولا في

```
whoami
```

---

## النتيجة

لسه

```text
Invalid input
```

---

يبقى عرفنا إن

```
;
```

هو نفسه ممنوع.

مش لازم أوصل لـ whoami أصلاً.

---

وده اسمه

## Root Cause Analysis

يعني أعرف السبب الحقيقي.

---

## بعد كده الكتاب ورالك مثال PHP

خلينا نشرحه.

الكود عامل كده تقريبًا:

```php
$blacklist = ['&', '|', ';'];
```

يعني عمل Array.

جواها كل الرموز الممنوعة.

---

بعدها

```php
foreach
```

يعني:

لف على كل عنصر.
![[Pasted image 20260705223402.png]]

---

هيبقى كأنه بيقول:

```
بص على ;

بعدين

بص على &

بعدين

بص على |
```

---

بعدها

```php
strpos(...)
```

دي Function في PHP.

وظيفتها:

تشوف

هل النص فيه الحرف ده؟

---

مثلاً

لو المستخدم بعت

```text
127.0.0.1;whoami
```

هيبص.

هل فيه

```
;
```

آه.

---

يبقى

```
Invalid input
```

---

ولو بعت

```text
127.0.0.1&whoami
```

برضو

هيلاقي

```
&
```

فيرفض.

---

## ليه Blacklist فكرة ضعيفة؟

ودي نقطة مهمة جدًا.

لأن المطور لازم يفتكر **كل** الطرق اللي المهاجم ممكن يستخدمها.

وده شبه مستحيل.

مثلاً منع:

```
;
```

ونسي:

```
&&
```

---

منع

```
&&
```

ونسي

```
|
```

---

منع

```
|
```

ونسي

New Line.

---

منع كل دول.

ونسي

```
$()
```

---

فكل مرة المهاجم يلاقي طريقة جديدة.

---

## علشان كده إحنا دلوقتي هنعمل إيه؟

مش هنفترض.

هنجرب.

واحدة واحدة.

---

بدل

```text
127.0.0.1;whoami
```

نجرب

```text
127.0.0.1&
```

---

بعدها

```text
127.0.0.1|
```

---

بعدها

```text
127.0.0.1&&
```

---

بعدها

```text
127.0.0.1||
```

---

بعدها

New Line.

---

ونشوف.

مين اللي بيتمنع.

ومين اللي بيعدي.

وده اسمه:

**Filter Enumeration**.

يعني بدل ما تقول "فيه فلتر"، تبدأ **ترسم حدود الفلتر**:

- إيه الرموز اللي بيمنعها؟
    
- إيه الكلمات اللي بيمنعها؟
    
- هل المنع Case Sensitive ولا لأ؟
    
- هل بيمنع قبل الـ URL Decoding ولا بعده؟
    

وده بالظبط التفكير اللي بيميز البنتستر: **مش بيخمن، لكنه بيختبر فرضية واحدة في كل مرة ويستنتج من النتيجة.**

# Layer 6

---

## إحنا وصلنا لفين؟

في الدرس اللي فات عرفنا إن الموقع بقى يعمل Filter.

يعني لما نبعت:

```text
127.0.0.1; whoami
```

يرد:

```text
Invalid Input
```

إحنا مش عارفين لسه هو مانع إيه.

هل مانع:

- `;`
    
- `whoami`
    
- Space
    
- ولا كلهم؟
    

فبدأنا نختبر واحدة واحدة.

---

## أول حاجة اكتشفناها

لقينا إن:

```text
;
```

ممنوعة.

يبقى الـ Semicolon متحظر.

---

## طيب هل ده معناه إن خلاص مفيش Injection؟

لا.

وده أهم درس.

**لو طريقة اتقفلت، مش معناها إن الثغرة اتصلحت.**

ممكن يكون المطور منع رمز واحد بس.

وأنا ألاقي رمز تاني.

---

## New Line

الكتاب قال:

خلينا نجرب الـ New Line.

يعني بدل:

```text
127.0.0.1;
```

يبقى فيه سطر جديد.

في الـ URL بيتكتب:

```text
%0a
```

---

## ليه الـ New Line ممكن يشتغل؟

خلينا نفتكر الـ Shell.

لما تكتب في الـ Terminal:

```bash
ls
pwd
```

إنت عمليًا كتبت:

- أمر
    
- ضغطت Enter
    
- أمر تاني
    

يعني الـ Enter نفسه بيفصل بين الأوامر.

فالـ New Line يعتبر فاصل أوامر طبيعي في الـ Shell.

---

## ليه المطور نسي يمنعه؟

لأن المطور غالبًا عمل Blacklist بالشكل ده:

```php
[';', '&', '|']
```

ومفكرش في:

```text
%0a
```

وده بيحصل كتير جدًا.

---

## النتيجة

لما بعت:

```text
127.0.0.1%0a
```

الموقع قبله.

ورجع نتيجة الـ Ping.

يبقى عرفنا إن:

**الـ New Line مش متفلتر.**

---

وده معناه إن عندنا Operator شغال.

لكن لسه محتاجين نحط أمر بعده.

---

## جربوا يضيفوا whoami

بقينا نبعت:

```text
127.0.0.1%0awhoami
```

(أو مع وجود مسافة بين الـ New Line والأمر حسب ما كانوا بيجربوا).

لكن الموقع رجع:

```text
Invalid Input
```
![[Pasted image 20260705224201.jpg]]

---

## السؤال

مين السبب؟

هل الـ New Line؟

لا.

لأنه قبله في التجربة اللي قبلها.

يبقى فيه حاجة جديدة.

---

## نرجع لنفس الطريقة العلمية

نفك الـ Payload.

كان عندنا:

```
127.0.0.1
```

بعدها

```
New Line
```

بعدها

```
Space
```

بعدها

```
whoami
```

يبقى نختبر العنصر اللي بعد الـ New Line مباشرة.

---

## اكتشفوا إن الـ Space هو المشكلة

وده منطقي جدًا.

ليه؟

لأن الـ Input المفروض يكون:

```text
127.0.0.1
```

هل المفروض IP يحتوي على مسافات؟

أكيد لا.

فالمطور قال:

> أي Request فيها Space = ارفض.

---

## طيب هل لازم أستخدم Space؟

هنا بقى يبدأ التفكير.

الهاكر بيسأل نفسه:

> "أنا محتاج Space... لكن هل لازم أكتبها بالشكل العادي؟"

والإجابة:

مش دايمًا.

---

## أول طريقة: Tab

في الكمبيوتر عندك زر اسمه:

```
Tab
```

وده مختلف عن الـ Space.

لكن الـ Shell بيعامله غالبًا كفاصل بين الكلمات، زي المسافة.

---

يعني لو كتبت في Terminal:

```bash
ls<Tab>-la
```

الـ Shell يفهمها كأنك كتبت:

```bash
ls -la
```

---

في الـ URL، الـ Tab بيتكتب:

```text
%09
```

---

يعني بدل:

```text
whoami␠
```

تبقى:

```text
whoami<Tab>
```

---

لو الفلتر بيدور على:

```
Space
```

مش هيلاقيها.

لكن الـ Shell هيفهمها.

---

وده سبب نجاحها.
![[Pasted image 20260705224333.jpg]]

---

## الطريقة الثانية: $IFS

ودي من أكتر الحاجات اللي الناس بتحفظها من غير ما تفهمها.

خلينا نفهمها.

---

## يعني إيه Environment Variable؟

Linux عنده متغيرات جاهزة.

زي:

```bash
HOME
PATH
USER
PWD
```

وفيه واحد اسمه:

```bash
IFS
```

---

## يعني إيه IFS؟

اختصار:

```
Internal Field Separator
```

وده متغير بيقول للـ Shell:

> لما تقسم الكلام إلى كلمات، استخدم إيه كفاصل؟

القيمة الافتراضية بتاعته غالبًا:

- Space
    
- Tab
    
- New Line
    

---

لو كتبت:

```bash
echo $IFS
```

مش هتشوف "Space" مكتوبة، لأن القيمة نفسها هي محارف الفصل (مسافة/تاب/سطر جديد).

---

لما تكتب:

```bash
cat${IFS}/etc/passwd
```

الـ Shell قبل التنفيذ يستبدل `${IFS}` بقيمة المتغير.

فتبقى كأنك كتبت:

```bash
cat /etc/passwd
```

---

لكن الفلتر كان بيدور على:

```
Space
```

وملقاش Space.

لقى:

```text
${IFS}
```

فقال:

> تمام.

---

وبعدين الـ Shell حولها لمسافة.

وده سبب نجاحها.
![[Pasted image 20260705224543.jpg]]

---

## الطريقة الثالثة: Brace Expansion

ودي بقى خاصية في Bash.

خلينا نفهمها.

---

لما تكتب:

```bash
{ls,-la}
```

الـ Bash يعمل حاجة اسمها:

Brace Expansion.

ويحولها إلى:

```bash
ls -la
```

بدون ما تكون كتبت Space بنفسك.

---

يعني أنت كتبت:

```text
{ls,-la}
```

والـ Bash فهمها:

```text
ls -la
```

---

يبقى الفلتر بيشوف:

```
{
}
,
```

لكن الـ Shell يشوف:

```
Space
```

---

وده سبب نجاحها.

---

## ليه كل الطرق دي شغالة؟

لاحظ إن كلهم مبنيين على نفس الفكرة.

الفلتر بيشتغل على **النص اللي إنت بعته**.

أما الـ Shell بيشتغل على **النص بعد ما يفسره**.

يعني فيه مرحلتين:

```
User Input
        ↓
Filter يشوف النص
        ↓
Shell يفسر النص
        ↓
Execution
```

المهاجم بيحاول يكتب حاجة:

- الفلتر يشوفها بشكل.
    
- لكن الـ Shell يفسرها بشكل مختلف.
    

وده جوهر معظم تقنيات الـ Filter Bypass.

---

## ليه الـ Blacklist ضعيف؟

لأن المطور لو قال:

```php
امنع Space
```

أنت عندك:

- Tab
    
- `${IFS}`
    
- Brace Expansion
    
- وطرق تانية كتير حسب الـ Shell والبيئة.
    

يعني كل مرة يمنع طريقة، ممكن تلاقي طريقة تانية تؤدي نفس الغرض.

وده السبب إن الاعتماد على **Blacklists** لوحدها بيكون ضعيف، بينما الحماية الأقوى هي **عدم تمرير مدخلات المستخدم إلى أوامر النظام أصلًا** أو استخدام **Allowlist (Whitelist)** والتحقق الصارم من المدخلات، مع تجنب استدعاء الـ Shell إذا كان فيه بديل آمن.



# Layer 7
الجزء ده من أكتر الأجزاء اللي بيخلي الناس تحفظ Payloads وخلاص، لكن لو فهمته هتعرف تخترع بنفسك طرق جديدة بدل ما تحفظها.

الفكرة الأساسية في الدرس كله هي:

> **أنا محتاج Character معين، لكن الفلتر مانعني أكتبه. فهل أقدر أخلي الـ Shell يطلعه بنفسه؟**

ودي هي الفكرة اللي هتتكرر في كل الأمثلة.

---

## المشكلة الجديدة

إحنا دلوقتي عرفنا إن الموقع ممكن يمنع:

```text
;
&
|
Space
```

لكن المطور ممكن يقول:

> "أنا همنع كمان الـ Slash."

يعني يمنع:

```text
/
```

أو على Windows:

```text
\
```

---

## ليه يمنع الـ Slash؟

لأن أي أمر بيتعامل مع ملفات تقريبًا بيحتاجه.

مثلاً:

```bash
cat /etc/passwd
```

أو

```bash
ls /home
```

أو

```bash
/var/www/html
```

كلهم فيهم `/`.

فالمطور يقول:

> لو شفت `/`... ارفض الريكوست.

---

## هل خلاص كده انتهينا؟

لا.

وهنا بقى يبدأ التفكير الصح.

## سؤال

هل لازم أكتب:

```text
/
```

بنفسي؟

ولا ممكن أخلي Bash يطلعه؟

الإجابة:

**أقدر أخليه يطلعه.**

---

## نرجع لموضوع Environment Variables

فاكر لما استخدمنا:

```bash
${IFS}
```

وقلنا إن الـ Shell بيبدله بمسافة.

يبقى فيه مبدأ مهم:

> الـ Shell بيستبدل قيمة المتغير قبل تنفيذ الأمر.

يعني لو كتبت:

```bash
echo $HOME
```

الـ Bash قبل ما يشغل `echo` هيحولها إلى:

```bash
echo /home/mohab
```

---

## PATH

خلينا نشوفه.

لو كتبت:

```bash
echo $PATH
```

ممكن يطلع:

```text
/usr/local/bin:/usr/bin:/bin:/usr/games
```

---

بص لأول حرف.

هو:

```text
/
```

صح؟

يبقى أنا عندي Slash موجود بالفعل.

لكن موجود فين؟

**جوه المتغير.**

---

## طيب يعني إيه

```bash
${PATH:0:1}
```

دي اسمها

## String Slicing

أو

Substring.

---

شكلها:

```bash
${VARIABLE:START:LENGTH}
```

يعني:

- هاتلي المتغير.
    
- ابدأ من الحرف رقم كام.
    
- وخد كام حرف.
    

---

## مثال بسيط

افرض عندك:

```text
Mohab
```

الحروف:

```text
M o h a b
0 1 2 3 4
```

---

لو كتبت:

```text
Start = 0
Length = 1
```

هيطلع:

```text
M
```

---

لو كتبت:

```text
Start = 2
Length = 2
```

هيطلع:

```text
ha
```

---

نفس الفكرة.

---

الـ PATH

قيمته:

```text
/usr/local/bin
```

أول حرف:

```text
/
```

يبقى:

```bash
${PATH:0:1}
```

=

```text
/
```

---

## طب الفلتر شاف إيه؟

دي أهم نقطة.

الفلتر شاف:

```text
${PATH:0:1}
```

مفيش Slash.

---

لكن الـ Bash شاف:

```text
/
```

---

وده سبب نجاح الطريقة.

---

## ليه استخدموا echo؟

الكتاب كتب:

```bash
echo ${PATH:0:1}
```

علشان يوريك الناتج.

لكن في الـ Payload الحقيقي

مش هنكتب:

```bash
echo
```

لأن إحنا مش عايزين نطبع.

إحنا عايزين الـ Character بس.

---

## HOME وPWD

الكتاب قال:

تقدر تستخدم

```bash
$HOME
```

أو

```bash
$PWD
```

برضو.

ليه؟

لأن قيمتهم غالبًا تبدأ بـ:

```text
/
```

مثلاً

```text
/home/mohab
```

أو

```text
/var/www
```

يبقى أول حرف برضو:

```text
/
```

---

## بعد كده دخل في حاجة أغرب

```bash
${LS_COLORS:10:1}
```

ورجعت:

```text
;
```

---

أول مرة تشوفها هتقول:

هو جاب الـ Semicolon منين؟

---

تعالى نفهم.

---

## LS_COLORS

ده Environment Variable.

قيمته طويلة جدًا.

مثلاً:

```text
rs=0:di=01;34:ln=01;36:pi=40;33
```

لاحظ.

في النص فيه:

```text
;
```

---

هو عمل إيه؟

قال:

هاتلي الحرف رقم 10.

بس.

---

طلع:

```text
;
```

---

يبقى هو مطلع:

```text
;
```

من جوه String موجودة بالفعل.

---

## الفكرة كلها

أنا مش كتبت:

```text
;
```

أنا طلبت حرف من متغير.

فالفلتر معداش عليه.

لكن Bash طلعه.

---

## الكتاب قال استخدم printenv

ليه؟

الأمر:

```bash
printenv
```

بيطبع كل Environment Variables.

مثلاً:

```text
PATH=...
HOME=...
PWD=...
USER=...
LANG=...
LS_COLORS=...
```

---

فأنت ممكن تدور.

مين فيهم فيه Character أنا محتاجه؟

وبعدين تاخده بالـ Substring.

---

وده بيخليك تبتكر Payloads بدل ما تحفظ.

---

## بعد كده عمل Payload

```text
127.0.0.1${LS_COLORS:10:1}${IFS}
```

خلينا نفكه.

---

أول جزء:

```text
127.0.0.1
```

---

بعده:

```bash
${LS_COLORS:10:1}
```

=

```text
;
```

---

بعده:

```bash
${IFS}
```

=

Space

---

يبقى الـ Shell شاف:

```text
127.0.0.1;
```

ثم مسافة.

---

لكن الفلتر شاف:

```text
${LS_COLORS:10:1}${IFS}
```

مفيش:

- `;`
    
- ولا Space
    

---

وده سبب نجاحه.

---

## Windows

نفس الفكرة.

لكن Syntax مختلفة.

---

## CMD

في Windows

فيه Variables.

زي:

```cmd
%HOMEPATH%
```

مثلاً قيمته:

```text
\Users\htb-student
```

---

أول Character:

```text
\
```

---

فـ CMD يسمحلك تعمل Substring.

يعني تقول:

```cmd
%HOMEPATH:~6,-11%
```

خلينا نفهمها.

---

شكلها:

```text
%VARIABLE:~Start,Length%
```

لكن في CMD الـ Length السالب معناه "اقطع من الآخر".

هو بيقول:

- ابدأ من الحرف رقم 6.
    
- واقطع آخر 11 حرف.
    

فالجزء اللي هيفضل هو:

```text
\
```

---

الفكرة هنا مش تحفظ الأرقام (6 و11)، لأنها هتختلف حسب قيمة المتغير على كل جهاز.

الفكرة إن **CMD بيدعم استخراج جزء من المتغير**.

---

## PowerShell

PowerShell أسهل.

ليه؟

لأنه بيعتبر الـ String عبارة عن Array.

يعني:

```text
Mohab
```

=

```text
M
o
h
a
b
```

---

فتقدر تقول:

```powershell
$env:HOMEPATH[0]
```

يعني:

هات أول Character.

---

لو قيمة HOMEPATH:

```text
\Users\Mohab
```

أول Character:

```text
\
```

---

يبقى رجعلك Backslash.

---

## Character Shifting

ودي من أجمل الأفكار.

---

بدل ما أجيب Character من Variable.

أنا أطلعه من Character تاني.

---

تعالى نفهم الأول ASCII.

كل حرف ليه رقم.

مثلاً:

```text
A = 65
B = 66
```

---

يبقى لو زودت 1 على:

```text
A
```

يبقى:

```text
B
```

---

الكتاب قال:

```text
\ = 92
```

وقبله مباشرة:

```text
[ = 91
```

---

يعني لو أخدت:

```text
[
```

وخليته يزيد واحد.

هيبقى:

```text
\
```

---

وده اللي بيعمله:

```bash
tr
```

---

## يعني إيه tr؟

Translate.

يعني:

غير Characters.

---

السطر:

```bash
tr '!-}' '"-~'
```

معقد شكله.

لكن فكرته ببساطة:

> كل Character هيتحول للحرف اللي بعده في ترتيب ASCII.

---

فهو بعت:

```text
[
```

والناتج:

```text
\
```

---

يعني مرة تانية...

أنا **مكتبتش Backslash**.

هو اتولد أثناء التنفيذ.

---

## التمرين الأخير

الكتاب بيقول:

طلعلي:

```text
;
```

بنفس الفكرة.

---

إزاي؟

أول حاجة لازم تعرف:

رقم:

```text
;
```

في ASCII.

هتلاقيه:

```text
59
```

---

الحرف اللي قبله:

```text
:
```

رقمه:

```text
58
```

---

يبقى لو عملت Character Shift على:

```text
:
```

هيطلع:

```text
;
```

وده نفس المبدأ اللي استخدمناه مع `[` و`\`.

---

## الخلاصة

الدرس كله مبني على فكرة واحدة:

```
User Input
      │
      ▼
Filter يفحص النص المكتوب
      │
      ▼
Shell يفسر المتغيرات والتعبيرات
      │
      ▼
يتولد الحرف الممنوع
      │
      ▼
تنفيذ الأمر
```

وده هو السبب إن **Blacklists لوحدها وسيلة حماية ضعيفة**. المطور بيمنع الحرف المكتوب، لكن الـ Shell يقدر يكوّن نفس الحرف بطرق مختلفة أثناء التنفيذ. علشان كده الحماية الصحيحة مش إنك تمنع رموز معينة، لكن إنك **متسمحش أصلًا بتمرير مدخلات المستخدم إلى أوامر النظام** أو تستخدم استدعاءات آمنة لا تعتمد على الـ Shell.


# Layer 8
الجزء ده مهم جدًا لأنه بينقلك من **Bypassing Characters** إلى **Bypassing Commands**.

في الجزء اللي فات كان الفلتر بيمنع **رموز** زي:

```text
;
/
\
Space
```

فإحنا كنا بنحاول نجيب نفس الرموز بطريقة تانية.

لكن دلوقتي الوضع اختلف.

الموقع مبقاش بيمنع Character.

بقى بيمنع **كلمة كاملة**.

وده فرق كبير.

---

## أولاً... يعني إيه Blacklisted Commands؟

نفترض إن المطور كتب كود زي ده:

```php
$blacklist = ['whoami', 'cat', 'ls'];

foreach ($blacklist as $word) {
    if (strpos($_POST['ip'], $word) !== false) {
        echo "Invalid input";
    }
}
```

خلينا نشرحه سطر سطر.

---

## أول سطر

```php
$blacklist = ['whoami', 'cat', 'ls'];
```

دي مجرد Array.

جواها كلمات ممنوعة.

يعني المطور بيقول:

> لو لقيت أي واحدة من الكلمات دي...
> 
> ارفض الطلب.

---

## بعد كده

```php
foreach ($blacklist as $word)
```

يعني:

لف على كل كلمة.

أول مرة

```text
whoami
```

تاني مرة

```text
cat
```

تالت مرة

```text
ls
```

---

## بعد كده

```php
strpos($_POST['ip'], $word)
```

دي معناها:

دور داخل الـ Input.

هل الكلمة دي موجودة؟

---

مثلاً المستخدم بعت:

```text
127.0.0.1 whoami
```

هيعمل:

```php
strpos("127.0.0.1 whoami","whoami")
```

هيلاقيها.

فيرجع مكانها.

---

لو المستخدم بعت

```text
127.0.0.1 cat
```

هيلاقي

```text
cat
```

---

لو بعت

```text
127.0.0.1 ls
```

هيلاقي

```text
ls
```

---

ولو لقاها

هيطبع

```text
Invalid Input
```

---

## طيب هل ده معناه انتهينا؟

لا.

ليه؟

لأن الفلتر بيدور على:

```text
whoami
```

بالشكل ده بالظبط.

Exact Match.

يعني نفس الحروف ورا بعض.

---

## السؤال المهم

لو قدرت أخلي Bash يشوف:

```text
whoami
```

لكن الفلتر ميشوفهاش...

يبقى كسبت.

وده اسمه:

## Command Obfuscation

---

## يعني إيه Obfuscation؟

الكلمة معناها:

إخفاء.

أو

تشويه الشكل.

لكن بدون تغيير المعنى.

---

مثال بعيد عن الأمن.

أنا كتبت:

```text
Mohab
```

لو كتبتها:

```text
Mo_hab
```

أو

```text
Mo-hab
```

أنا غيرت شكلها.

لكن لسه أنت عرفت تقراها.

---

نفس الفكرة هنا.

أنا هغير شكل الأمر.

لكن الـ Shell هيشوفه زي ما هو.

---

## أول طريقة

Single Quotes

الكتاب كتب:

```bash
w'h'o'am'i
```

أول مرة تشوفها هتقول:

إزاي دي تشتغل؟

---

خلينا نفهم Bash.

---

في Bash

لما تكتب:

```bash
'hello'
```

الـ Quotes مش جزء من الكلمة.

دي مجرد Delimiters.

يعني بتحدد بداية ونهاية نص.

---

مثلاً

```bash
echo 'hello'
```

هيطبع

```text
hello
```

مش

```text
'hello'
```

---

الـ Quotes نفسها بتختفي.

---

طيب لو كتبت

```bash
w'h'o'am'i
```

خلينا نقسمها.

Bash شايف:

```text
w
```

وبعدين

```text
'h'
```

وبعدين

```text
'o'
```

وبعدين

```text
'am'
```

وبعدين

```text
'i'
```

---

بعد ما يشيل الـ Quotes

يبقى:

```text
whoami
```

---

يبقى Bash نفذه عادي.

---

لكن الفلتر شاف إيه؟

شاف:

```text
w'h'o'am'i
```

---

هل دي تساوي:

```text
whoami
```


لا.

يبقى معدتش على الـ Blacklist.

---

وده سبب نجاحها.

---

## نفس الفكرة بالـ Double Quotes

```bash
w"h"o"am"i
```

---

Bash بيشيل

```text
"
```

ويبقى:

```text
whoami
```

---

فالنتيجة واحدة.

---

## ليه قال

متخلطش Quotes؟

يعني مينفعش:

```bash
w'h"o'am'i
```

---

ليه؟

لأن Bash ليه Rules.

كل Quote لازم يتقفل بنفس النوع.

---

مثلاً

```bash
'
```

لازم يتقفل بـ

```bash
'
```

مش

```bash
"
```

---

ولازم العدد يبقى زوجي.

يعني:

```text
''
```

أو

```text
""""
```

---

لكن

```text
'''
```

غلط.

---

## بعد كده جربوه

عملوا Payload

```
127.0.0.1
(New Line)
w'h'o'am'i
```

الفلتر عدى.

والـ Shell نفذ

```bash
whoami
```

---

وده معناه إن الـ Blacklist كان بيعمل Match للكلمة فقط.

---

## Linux Only

بعد كده دخل في حاجتين.

---

## أول واحدة

```bash
who$@ami
```

---

لازم الأول نفهم

يعني إيه

```bash
$@
```

---

في Bash

لما تعمل Script

مثلاً

```bash
test.sh
```

وشغلته

```bash
./test.sh a b c
```

---

يبقى

```text
$1 = a

$2 = b

$3 = c
```

---

أما

```bash
$@
```

فمعناه:

كل الـ Arguments.

---

طيب هنا مفيش Arguments.

يبقى

```bash
$@
```

قيمته إيه؟

Empty.

يعني ولا حاجة.

---

يبقى

```bash
who$@ami
```

=

```bash
who
```

Nothing

```bash
ami
```

=

```bash
whoami
```

---

لكن الفلتر شاف:

```text
who$@ami
```

مش

```text
whoami
```

---

وده سبب نجاحها.

---

## الطريقة التانية

Backslash

```bash
w\ho\am\i
```

---

دي محتاجة نفهم الـ Escape.

---

في Bash

الـ

```text
\
```

بيستخدم للهروب.

Escape.

---

يعني

```bash
\a
```

معناها:

اعتبر

```text
a
```

Character عادي.

---

فلما تكتب

```bash
w\ho\am\i
```

الـ Bash يشيل

```text
\
```

ويبقى:

```text
whoami
```

---

لكن الفلتر شاف

```text
w\ho\am\i
```

---

فمش هيطابق

```text
whoami
```

---

وده سبب نجاحها.

---

## التمرين

الكتاب قال:

جرب

```bash
who$@ami
```

أو

```bash
w\ho\am\i
```

---

ولو منفعوش...

اسأل نفسك.

ليه؟

---

يمكن

```text
\
```

ممنوعة.

---

طب أعمل إيه؟

ارجع للدرس اللي فات.

واستخدم طريقة تولد بيها

```text
\
```

من Environment Variable.

---

لاحظ إن الدروس كلها مرتبطة ببعض.

---

## Windows Only

في Windows

فيه Character اسمه

Caret

```text
^
```

---

لو كتبت

```cmd
who^ami
```

CMD هيشيل

```text
^
```

ويبقى

```text
whoami
```

---

لكن الفلتر شاف

```text
who^ami
```

---

فمش هيعمل Match.

---

## ليه كل الطرق دي شغالة؟

كل الطرق مبنية على نفس الفكرة.

الفلتر بيشتغل على **النص الخام (Raw Input)**.

لكن الـ Shell قبل التنفيذ بيعمل Parsing.

يعني:

```text
User Input
        │
        ▼
Filter يشوف:
w'h'o'am'i
        │
        ▼
Bash يعمل Parsing
        │
        ▼
يشيل الـ Quotes
        │
        ▼
whoami
        │
        ▼
تنفيذ الأمر
```

---

## الفرق بين Bypassing Characters وBypassing Commands

في الجزء اللي فات كنا بنقول:

> "أنا محتاج أكتب `;` لكن الفلتر مانعه."

فكنا بنحاول **نولد Character** بطريقة تانية.

أما في الجزء ده فالمشكلة مختلفة:

> "الفلتر بيدور على كلمة `whoami`."

فبقينا نحاول **نغير شكل الكلمة** من غير ما نغير معناها بالنسبة للـ Shell.

وده هو مفهوم **Command Obfuscation**: تخلي الأمر يبدو مختلفًا للفلتر، لكنه يظل نفس الأمر عندما يفسره الـ Shell. فهم المبدأ ده أهم بكتير من حفظ الأمثلة، لأن طرق الإخفاء بتختلف حسب نوع الـ Shell ونظام التشغيل.


----



# Layer 9
الجزء ده يعتبر **أقوى جزء في الـ Filter Bypass**، لأنه بيتكلم عن الحالات اللي الفلاتر العادية أو حتى بعض الـ WAFs بتكون أذكى، فمش بيكفي إنك تضيف Quote أو `$@`. الفكرة هنا مش إننا "نخبي" الأمر بحرفين، لكن إننا **نغير شكله بالكامل** بحيث الفلتر ميعرفش يتعرف عليه، بينما الـ Shell في النهاية يرجعه لنفس الأمر.

خلينا نشرحه من الأول.

---

## أولاً: يعني إيه Advanced Command Obfuscation؟

إحنا قبل كده كنا بنعمل حاجات بسيطة زي:

```bash
w'h'o'am'i
```

أو

```bash
who$@ami
```

ودي كانت بتشتغل مع Blacklists البسيطة.

لكن تخيل إن فيه WAF بيعمل تحليل أذكى، ومش بيدور بس على كلمة `whoami`، لكن بيحاول يشيل الـ Quotes أو يفسر بعض الحيل.

ساعتها محتاجين نغير شكل الأمر بطريقة أكبر.

---

## أول تقنية: Case Manipulation

يعني نغير حالة الحروف.

بدل:

```text
whoami
```

نكتب:

```text
WHOAMI
```

أو

```text
WhOaMi
```

---

## ليه ده ممكن يشتغل؟

تخيل إن المطور كتب فلتر بالشكل ده:

```php
if (strpos($input, "whoami") !== false)
```

هو بيدور على:

```text
whoami
```

بالظبط.

لكن:

```text
WHOAMI
```

مش نفس الـ String.

فـ `strpos()` مش هتلاقيها.

---

## طب هل الأمر هيتنفذ؟

هنا لازم نفرق بين Linux وWindows.

---

## Windows

في Windows (سواء CMD أو PowerShell)، الأوامر **Case-Insensitive**.

يعني:

```powershell
whoami
WHOAMI
WhOaMi
wHoAmI
```

كلهم نفس الحاجة.

يعني Windows مش بيفرق بين الكبير والصغير.

---

## Linux

أما Linux فـ **Case-Sensitive**.

يعني:

```bash
whoami
```

أمر موجود.

لكن:

```bash
WHOAMI
```

غالبًا هيقول:

```text
command not found
```

لأن مفيش أمر اسمه `WHOAMI`.

---

## طيب أعمل إيه في Linux؟

الكتاب قال:

هنبعت الأمر بالشكل:

```text
WhOaMi
```

وبعدين نخلي Bash يحوله قبل التنفيذ إلى:

```text
whoami
```

---

## تعال نفهم السطر ده

```bash
$(tr "[A-Z]" "[a-z]"<<<"WhOaMi")
```

شكله يخوف، لكن هو بسيط جدًا.

نفكه.

---

### أول جزء

```bash
tr
```

ده أمر اسمه Translate.

وظيفته:

يستبدل Characters بCharacters تانية.

---

يعني مثلاً:

```bash
tr A B
```

يعني:

كل A خليها B.

---

### هنا هو قال

```bash
tr "[A-Z]" "[a-z]"
```

يعني:

أي حرف Capital

حوله لـ Small.

---

يعني:

```text
WhOaMi
```

تبقى:

```text
whoami
```

---

### طيب يعني إيه

```bash
<<<
```

دي اسمها:

Here String.

يعني بدل ما تقرأ من File.

اقرأ من النص اللي بعده.

يعني:

```bash
tr "[A-Z]" "[a-z]"<<<"WhOaMi"
```

معناها:

حول النص:

```text
WhOaMi
```

إلى:

```text
whoami
```

---

### بعد كده

لفها بـ

```bash
$()
```

يعني:

نفذ الناتج.

يبقى Bash يعمل الخطوات دي:

```
WhOaMi
      │
      ▼
tr
      │
      ▼
whoami
      │
      ▼
$()
      │
      ▼
نفذ whoami
```

---

## ليه الموقع رفضها أول مرة؟

الكتاب قال:

رغم إن الفكرة صح...

الموقع رفض.

ليه؟

لأن الأمر كان فيه:

```
Space
```

وإحنا عارفين من الدرس اللي فات إن الموقع مانع الـ Space.

يعني المشكلة مش في التقنية.

المشكلة في Character Filter.

---

فلما استبدلوا الـ Space بـ:

```
Tab (%09)
```

اشتغل.

---

وده درس مهم جدًا.

### لما Payload يفشل...

متستنتجش إن الفكرة غلط.

راجع الأول:

- هل استخدمت Character ممنوع؟
    
- هل فيه Space؟
    
- هل فيه Semicolon؟
    
- هل فيه Slash؟
    

---

## الطريقة التانية

```bash
$(a="WhOaMi";printf %s "${a,,}")
```

نفكها.

---

أول حاجة

```bash
a="WhOaMi"
```

يعني:

اعمل Variable.

---

بعدها

```bash
${a,,}
```

دي خاصية في Bash.

معناها:

حول كل الحروف لـ Lowercase.

---

يعني:

```
WhOaMi
```

↓

```
whoami
```

---

بعدها

```bash
printf
```

يطبع الناتج.

---

وبعدين

```bash
$()
```

ينفذه.

---

يبقى نفس الفكرة.

لكن بطريقة مختلفة.

---

## التقنية التانية

Reversed Commands

ودي من أكتر الطرق الذكية.

---

بدل ما أكتب:

```
whoami
```

هكتبها بالمقلوب.

```
imaohw
```

---

الفلتر بيدور على:

```
whoami
```

هل موجودة؟

لا.

---

يبقى يعدي.

100074136936
1

---

## أجيب الكلمة بالمقلوب إزاي؟

```bash
echo whoami | rev
```

أمر

```bash
rev
```

بيعكس النص.

---

يبقى:

```
whoami
```

↓

```
imaohw
```

---

## طيب أشغلها إزاي؟

السطر ده:

```bash
$(rev<<<'imaohw')
```

خلينا نفكه.

---

الـ

```bash
rev
```

هيعكس:

```
imaohw
```

↓

```
whoami
```

---

بعدها

```bash
$()
```

تنفذ الناتج.

---

يبقى:

```
imaohw
     │
     ▼
rev
     │
     ▼
whoami
     │
     ▼
تنفيذ
```

---

لاحظ إن كلمة:

```
whoami
```

موجودة فين؟

ولا في حتة.

هي اتكونت وقت التنفيذ.

---

## Tip مهم

الكتاب قال:

لو كنت مستخدم Characters ممنوعة.

اقلبها هي كمان.

---

مثلاً لو عندك:

```
cat /etc/passwd
```

مش هتعكس كلمة `cat` بس.

هتعكس السطر كله.

علشان كل الـ Characters ترجع صح بعد الـ rev.

---

## Windows

في PowerShell.

عكس الـ String بيتم بالطريقة دي:

```powershell
"whoami"[-1..-20]
```

ليه؟

لأن PowerShell بيعتبر الـ String Array.

---

```
whoami
```

=

```
w
h
o
a
m
i
```

---

ولما تمشي بالعكس.

يبقى:

```
imaohw
```

---

بعدها

```powershell
iex
```

---

يعني إيه

```powershell
iex
```

؟

اختصار:

```
Invoke-Expression
```

وده يعادل تقريبًا:

```bash
$()
```

في Bash.

يعني:

نفذ الـ String كأمر.

---

## آخر تقنية

Encoded Commands

ودي من أشهر الطرق.

---

بدل ما أكتب:

```bash
cat /etc/passwd
```

أخزنها بشكل مش مفهوم.

مثلاً:

Base64.

---

## يعني إيه Base64؟

طريقة لتحويل أي نص إلى نص مختلف.

مثلاً:

```
whoami
```

تبقى:

```
d2hvYW1p
```

---

دي مش Encryption.

دي مجرد Encoding.

يعني تقدر ترجعها بسهولة.

---

## ليه نستخدمها؟

علشان الفلتر بيدور على:

```
cat
```

أو

```
passwd
```

---

لكن مش هيلاقي:

```
Y2F0IC9ldGM...
```

---

يبقى يعدي.

---

## بعدين؟

وقت التنفيذ.

نفك الـ Base64.

---

السطر:

```bash
base64 -d
```

معناه:

Decode.

---

بعدها:

```bash
bash<<<
```

يعني:

نفذ الناتج.

---

يبقى الخطوات:

```
Base64 String
        │
        ▼
Decode
        │
        ▼
cat /etc/passwd
        │
        ▼
bash
        │
        ▼
تنفيذ
```

---

## ليه استخدم

```bash
<<<
```

بدل

```bash
|
```

؟

لأن:

```
|
```

Pipe.

وده غالبًا ممنوع.

فاستبدله بـ Here String.

---

## Windows Base64

في PowerShell.

الـ Base64 مختلف شوية.

ليه؟

لأن PowerShell بيشتغل غالبًا بـ UTF-16.

مش UTF-8.

---

علشان كده لو هتولد Base64 من Linux.

لازم الأول تعمل:

```
UTF-8
```

↓

```
UTF-16LE
```

باستخدام:

```bash
iconv
```

---

بعدها تعمل Base64.

---

ثم في PowerShell:

```
Decode
```

↓

```
iex
```

↓

تنفيذ.

---

## الخلاصة

كل التقنيات اللي في الجزء ده هدفها واحد، لكن كل واحدة بتستخدم أسلوب مختلف:

|التقنية|الفكرة|
|---|---|
|**Case Manipulation**|تغيير شكل الحروف (Upper/Lower) ثم إرجاعها للشكل المطلوب قبل التنفيذ.|
|**Reversed Commands**|إرسال الأمر بالمقلوب ثم قلبه مرة تانية وقت التنفيذ.|
|**Base64 Encoding**|إرسال الأمر مشفر بـ Base64 ثم فك التشفير وقت التنفيذ وتشغيله.|

لاحظ إن في كل الحالات **الأمر الحقيقي (`whoami` أو `cat` أو غيره) لا يكون موجودًا بصورته الأصلية في المدخلات التي يراها الفلتر**، لكنه يتكوّن داخل الـ Shell أثناء التنفيذ. وده هو جوهر **Advanced Command Obfuscation**: تغيير شكل الأمر بحيث يصعب على الفلاتر أو بعض الـ WAFs التعرف عليه، مع الحفاظ على نفس النتيجة النهائية عند التنفيذ.


# Layer 10
الجزء ده مش بيتكلم على Technique جديدة، لكنه بيقول:

> **لو الفلاتر أو الـ WAF بقى ذكي جدًا، بدل ما تقعد تكتب Obfuscation بإيدك، استخدم Tool تعمله أوتوماتيك.**

يعني بدل ما تقعد تفكر:

- أحط Quotes؟
    
- أستخدم `$IFS`؟
    
- أقلب الكلمة؟
    
- أعمل Base64؟
    

فيه Tools بتعمل كل ده لوحدها، وأحيانًا بتستخدم عشرات التقنيات مع بعض.

---

## ليه أصلاً محتاج Tool؟

إحنا لحد دلوقتي عملنا حاجات زي:

```
w'h'o'am'i
```

أو

```
who$@ami
```

أو

```
$(rev<<<imaohw)
```

أو

```
bash<<<$(base64 ...)
```

دي كلها Obfuscation.

لكن تخيل إن الـ WAF بقى بيعرف الطرق دي كلها.

هتعمل إيه؟

ممكن تقعد ساعة تكتب Payload جديد.

أو تستخدم Tool تطلعلك آلاف الأشكال المختلفة لنفس الأمر.

وده اللي Bashfuscator و DOSfuscation بيعملوه.

---

## أول Tool

## Bashfuscator

ده خاص بـ Linux (Bash).

فكر فيه كأنه:

> **Command Generator**

بتديله أمر عادي.

وهو يرجعلك نفس الأمر لكن شكله مستحيل حد يفهمه.

---

## التثبيت

الكتاب بيقول:

```bash
git clone https://github.com/Bashfuscator/Bashfuscator
```

يعني حمل المشروع من GitHub.

---

بعدها:

```bash
cd Bashfuscator
```

ادخل الفولدر.

---

بعدها:

```bash
pip3 install setuptools==65
```

يثبت Dependency محتاجها البرنامج.

---

بعدها:

```bash
python3 setup.py install --user
```

يثبت البرنامج عندك.

---

## بعد كده؟

تدخل على

```bash
./bashfuscator/bin
```

وهتشغل:

```bash
./bashfuscator
```

---

## الـ Help

```bash
./bashfuscator -h
```

زي أي Tool.

يعرضلك كل الـ Options.

---

هتلاقي مثلًا:

```
-c
```

يعني:

Command.

---

يعني:

```
-c "whoami"
```

يعني:

اعمل Obfuscation للأمر ده.

---

## أول تجربة

مثلاً:

```bash
./bashfuscator -c "cat /etc/passwd"
```

---

الـ Tool هيطلعلك حاجة بالشكل ده:

```
${*/+27\[X\(}.......
```

أو

```
${*~}....
```

أو أي حاجة شكلها مرعب.

---

الفكرة هنا إن:

الـ Payload ممكن يبقى

**1000 حرف**

أو

**5000 حرف**

أو

**100 ألف حرف**

حسب الـ Technique اللي اختارها.

---

## ليه بيطلع طويل؟

لأن البرنامج بيختار Technique عشوائيًا.

مثلاً:

- Reverse
    
- Variables
    
- Arithmetic Expansion
    
- String Expansion
    
- Eval
    
- Base64
    
- Parameter Expansion
    

ويمكن يستخدمهم كلهم مع بعض.

---

## طيب لو أنا عايز Payload صغير؟

هنا استخدم Options.

زي:

```bash
-s 1
```

يعني:

Size صغير.

---

```
-t 1
```

يعني:

Technique بسيطة.

---

```
--layers 1
```

يعني:

اعمل Layer واحدة بس.

بدل ما يلفها عشر مرات.

---

```
--no-mangling
```

يعني:

متعملش تعقيد زيادة.

---

فيطلعلك Payload زي ده:

```bash
eval "$(W0=(w \ t e c p s a \/ d);for ... )"
```

---

شكله غريب؟

آه.

بس مقارنة باللي قبله صغير جدًا.

---

## تعال نفهم الـ Payload

مثلاً:

```
W0=(w \ t e c p s a \/ d)
```

هو عمل Array.

كل Character لوحده.

---

يعني بدل ما يكتب:

```
cat
```

كتب:

```
c
a
t
```

متفرقين.

---

وبعدين باستخدام Loop.

يرجعهم بالترتيب.

---

وبعدين:

```
printf
```

يجمعهم.

---

وبعدين:

```
eval
```

يشغلهم.

---

يعني:

```
Characters
      │
      ▼
Array
      │
      ▼
Loop
      │
      ▼
String
      │
      ▼
eval
      │
      ▼
تنفيذ
```

---

وده سبب إن الـ WAF ميعرفش يشوف كلمة:

```
cat
```

لأنها عمرها ما كانت مكتوبة.

---

## طيب أجربه إزاي؟

الكتاب قال:

شغله داخل Bash.

يعني:

```bash
bash -c 'payload'
```

---

يعني Bash ينفذ الـ Payload.

---

لو رجعلك:

```
/etc/passwd
```

يبقى الـ Payload سليم.

---

## بعد كده؟

ابعته للموقع.

وشوف.

---

## الكتاب قال Exercise

جربه على الموقع.

---

ولو مشتغلش.

فكر ليه.

---

الإجابة غالبًا:

لأن الـ Tool ممكن يكون استخدم Character الموقع مانعه.

مثلاً:

```
Space
```

أو

```
;
```

أو

```
|
```

أو

```
/
```

---

يبقى المشكلة مش في Bashfuscator.

المشكلة إنك لازم تغير الـ Options لحد ما يطلع Payload يناسب الفلتر.

---

## تاني Tool

DOSfuscation

---

وده خاص بـ Windows.

---

فكر فيه إنه:

Bashfuscator

لكن لـ CMD و PowerShell.

---

## التثبيت

يحمله من GitHub.

---

بعدها:

```powershell
Import-Module
```

يعني:

حمل الـ Module داخل PowerShell.

---

بعدها:

```powershell
Invoke-DOSfuscation
```

يفتح الـ Tool.

---

هتلاقي Prompt جديد:

```
Invoke-DOSfuscation>
```

---

وده Interactive.

يعني بتتكلم معاه.

---

## أول أمر

```
SET COMMAND
```

يعني:

قول للبرنامج.

إيه الأمر اللي عايزه يخبّيه؟

مثلاً:

```
SET COMMAND type C:\Users\flag.txt
```

---

يعني:

عايز أخفي الأمر ده.

---

بعدها:

```
encoding
```

يعني:

اختار طريقة Obfuscation.

---

بعدها:

```
1
```

اختار أول Encoding.

---

هيطلعلك أمر بالشكل ده:

```
typ%TEMP:~-3,-2%
```

ويكمل.

---

لو بصيت عليه.

هتحس إنه عشوائي.

---

لكن الحقيقة.

هو بيستخدم Environment Variables.

---

مثلاً:

```
%TEMP%
```

قيمتها مثلاً:

```
C:\Users\Admin\AppData\Local\Temp
```

---

هو بياخد منها Character معين.

---

بعدها:

```
%SystemRoot%
```

ياخد Character.

---

بعدها:

```
%TMP%
```

ياخد Character.

---

وفي الآخر.

يجمعهم.

---

فالـ CMD يشوف:

```
type
```

لكن الـ WAF عمره ما شاف كلمة:

```
type
```

---

## ليه الطريقة دي قوية؟

لأن:

الفلتر بيدور على:

```
type
```

لكن اللي وصله:

```
typ%TEMP:~-3,-2%
```

---

مش هيعتبرها Command معروف.

---

لكن وقت التنفيذ.

Windows هيبدل:

```
%TEMP%
```

بقيمة الـ Variable.

---

ويبقى الناتج النهائي:

```
type
```

---

## PowerShell على Linux؟

الكتاب قال حاجة لطيفة.

لو معندكش Windows.

مش مشكلة.

---

شغل:

```bash
pwsh
```

وده PowerShell على Linux.

---

وجرب نفس الأوامر.

---

## ليه نتعلم Tools دي طالما ممكن أكتب Payload بإيدي؟

لأن في الواقع العملي:

- بعض الـ WAFs بتكتشف الحيل البسيطة.
    
- بعض الفلاتر بتمنع عشرات الـ Characters والكلمات في نفس الوقت.
    
- بعض البيئات تحتاج مئات المحاولات للوصول إلى Payload مناسب.
    

أدوات مثل **Bashfuscator** و **Invoke-DOSfuscation** توفر وقتًا كبيرًا لأنها تولد أشكالًا مختلفة لنفس الأمر باستخدام تقنيات متعددة (تقسيم السلاسل، المتغيرات، إعادة الترتيب، الترميز، وغيرها)، ويمكنك تجربة أكثر من Payload حتى تجد واحدًا يمر عبر الفلاتر. مع ذلك، هذه الأدوات ليست "سحرية"؛ فإذا كان الموقع يمنع Characters أو Constructs معينة استخدمتها الأداة، فقد تحتاج لتغيير إعداداتها أو اختيار أسلوب Obfuscation مختلف.


-------
الجزء ده هو **الجانب الدفاعي (Blue Team / Secure Coding)**. يعني بعد ما اتعلمت كمهاجم إزاي تستغل Command Injection وتعدي الفلاتر، دلوقتي هنتكلم كمطور أو Security Engineer: **إزاي تمنعها من الأساس؟**

وفيه نقطة مهمة جدًا:

> معظم الناس بتفكر إن الحل هو "اعمل Blacklist للكلمات". وده غلط.
> 
> الحل الحقيقي هو إنك **متخليش الـ User يقدر يأثر على System Commands أصلاً**.

خلينا نمشي جزء جزء.

---

## أولاً: System Commands

الكتاب بدأ بأهم نصيحة:

> **بلاش تستخدم أوامر الـ Operating System طالما فيه بديل.**

وده أهم سطر في الدرس كله.

---

## مثال

نفترض إن عندك موقع.

وفيه زرار:

```
Check Host
```

ولما المستخدم يكتب:

```
8.8.8.8
```

الموقع يعمل:

```php
system("ping -c 1 ".$ip);
```

يعني PHP تطلع بره اللغة.

وتروح تقول للـ Linux:

```
شغل ping
```

---

المشكلة هنا إنك خرجت من بيئة PHP الآمنة.

ودخلت عالم الـ Shell.

وأي حاجة تدخل الـ Shell معرضة لـ Command Injection.

---

## طب أعمل إيه؟

الكتاب بيقول:

استخدم Functions موجودة في اللغة نفسها.

مثلاً في PHP.

بدل:

```php
system("ping ...");
```

استخدم:

```php
fsockopen()
```

---

## يعني إيه fsockopen؟

دي Function في PHP.

بتعمل اتصال بالشبكة مباشرة.

يعني بدل ما تقول للـ Linux:

```
شغل ping
```

PHP نفسها تعمل الاتصال.

وبالتالي مفيش Shell أصلاً.

فمفيش Command Injection.

---

وده ينطبق على أي لغة.

يعني لو فيه Function Built-in تعمل المطلوب.

استخدمها.

---

## ليه؟

لأن Built-in Functions

بتكون:

- أسرع
    
- أكثر أمانًا
    
- أقل عرضة للهجوم
    

---

## طيب لو مضطر أستخدم system()؟

أحيانًا فعلًا مفيش بديل.

مثلاً برنامج لازم يتشغل من الـ OS.

ساعتها الكتاب بيقول:

يبقى:

1. Validate
    
2. Sanitize
    

ومتحطش Input المستخدم مباشرة.

---

## ثانياً: Input Validation

ودي أول مرحلة.

---

## يعني إيه Validation؟

Validation يعني:

> أتأكد إن الـ Input بالشكل اللي أنا متوقعه.

مش أكتر.

---

مثلاً.

أنا مستني:

IP

يبقى مينفعش المستخدم يكتب:

```
hello
```

ولا:

```
abc
```

ولا:

```
cat
```

ولا:

```
127.0.0.1;whoami
```

---

لازم يكون:

```
192.168.1.1
```

بس.

---

## مثال PHP

```php
filter_var($_GET['ip'], FILTER_VALIDATE_IP)
```

تعال نفكها.

---

### filter_var()

دي Function.

وظيفتها:

تفحص قيمة معينة.

---

### FILTER_VALIDATE_IP

يعني:

اتأكد إن القيمة دي IP صحيح.

---

لو المستخدم كتب:

```
127.0.0.1
```

ترجع:

```
True
```

---

لو كتب:

```
abc
```

ترجع:

```
False
```

---

لو كتب:

```
127.0.0.1;whoami
```

برضو:

```
False
```

---

وساعتها ترفض الطلب.

---

## ليه دي أحسن من Regex؟

مش شرط تكون أحسن.

لكن:

PHP عاملالك Function جاهزة.

فاستخدمها.

بدل ما تكتب Regex معقد.

---

## طب لو الـ Input مش IP؟

مثلاً:

رقم تليفون.

أو

Username.

أو

Student ID.

---

ساعتها تستخدم:

Regex.

---

## يعني إيه Regex؟

هو Pattern.

يعني قانون.

مثلاً:

```
لازم يبدأ بحرف

بعده 5 أرقام
```

---

أي حاجة متطابقش.

ترفضها.

---

## NodeJS

في JavaScript.

هو كتب Regex كبير.

وظيفته:

يتأكد إن كل رقم في الـ IP

من 0 لـ255.

---

وده نفس Validation.

---

## Libraries

الكتاب قال:

بدل ما تكتب Regex بإيدك.

في Libraries جاهزة.

زي:

```
is-ip
```

---

أنت تعمل:

```
npm install is-ip
```

---

وبعدين:

```javascript
isIp(ip)
```

---

ترجع:

True

أو

False.

---

وده أحسن.

لأن حد غيرك راجع الكود.

---

## ثالثاً: Input Sanitization

ودي أهم من Validation.

---

في ناس بتتلخبط.

إيه الفرق؟

---

Validation

يعني:

```
هل الـ Input شكله صح؟
```

---

Sanitization

يعني:

```
اشيل أي حاجة مش محتاجها.
```

---

مثال.

أنا مستني:

IP.

---

المستخدم بعت:

```
127.0.0.1
```

تمام.

---

لكن بعت:

```
127.0.0.1;;;;;
```

---

ممكن الـ Validation يعديها بسبب Bug.

---

لكن الـ Sanitization هيشيل:

```
;;;;;
```

---

ويبقى:

```
127.0.0.1
```

---

## مثال

```php
preg_replace()
```

---

دي Function.

اسمها:

Regular Expression Replace.

---

يعني:

استبدل أي حاجة تطابق الـ Pattern.

---

السطر:

```php
preg_replace('/[^A-Za-z0-9.]/','',$ip)
```

نفكه.

---

الجزء ده:

```
A-Z
```

كل الحروف الكبيرة.

---

```
a-z
```

الحروف الصغيرة.

---

```
0-9
```

الأرقام.

---

```
.
```

النقطة.

---

طيب علامة

```
^
```

هنا معناها:

NOT

---

يبقى:

```
[^A-Za-z0-9.]
```

=

أي Character

مش

حرف

ولا رقم

ولا نقطة.

---

استبدله بـ:

```
''
```

يعني امسحه.

---

مثلاً.

المستخدم كتب:

```
127.0.0.1;whoami
```

---

هيبقى:

```
127.0.0.1whoami
```

---

يعني الـ

```
;
```

اختفت.

---

كتب:

```
127.0.0.1&&ls
```

---

يبقى:

```
127.0.0.1ls
```

---

كتب:

```
127.0.0.1|cat
```

---

يبقى:

```
127.0.0.1cat
```

---

كل الـ Special Characters راحت.

---

## JavaScript

نفس الفكرة.

```javascript
replace()
```

---

بيشيل أي Character.

مش حرف.

ولا رقم.

ولا نقطة.

---

## DOMPurify

الكتاب ذكر Library اسمها:

DOMPurify.

---

دي مش معمولة أساسًا لـ Command Injection.

هي معمولة لتنضيف الـ HTML ومنع XSS.

لكنها تقدر تساعد في تنظيف بعض المدخلات في حالات معينة.

ومع ذلك، لو هدفك حماية أوامر النظام، الأفضل تستخدم Validation وSanitization مناسبة لنوع البيانات، ومتستخدمش DOMPurify كحل أساسي ضد Command Injection.

---

## Escaping

الكتاب اتكلم عن حاجة اسمها:

Escaping.

---

يعني بدل ما تمسح:

```
;
```

---

تعملها Escape.

بحيث الـ Shell تعتبرها:

Character عادي.

---

مثلاً.

```
;
```

تبقى:

```
\;
```

---

لكن الكتاب قال:

متعتمدش عليه.

---

ليه؟

لأن:

إحنا طول الموديول

كنا بنتعلم

إزاي نعدي الـ Escaping.

---

يعني.

Escaping

طبقة حماية.

مش حماية كاملة.

---

## رابعاً

Server Configuration

وده معناه:

حتى لو فيه Bug.

خلي تأثيره قليل.

---

## أول حاجة

Web Application Firewall

زي:

Apache

فيه:

```
mod_security
```

---

وده بيفحص الـ Requests.

---

وكمان.

ممكن تستخدم WAF خارجي.

زي:

- Cloudflare
    
- Fortinet
    
- Imperva
    

---

وظيفتهم:

يشوفوا الـ Request.

ولو فيها Attack.

يوقفوها.

---

## Principle of Least Privilege (PoLP)

ودي من أهم مبادئ الأمن.

---

معناها:

> أي برنامج ياخد أقل صلاحيات ممكنة.

---

يعني.

الموقع يشتغل بـ:

```
www-data
```

مش:

```
root
```

---

ليه؟

تخيل حصل Command Injection.

---

لو الموقع شغال Root.

المهاجم هيبقى Root.

---

لكن لو شغال:

```
www-data
```

يبقى صلاحياته محدودة.

---

يعني حتى لو اخترق الموقع.

الخسارة أقل.

---

## disable_functions

في PHP.

تقدر تمنع Functions معينة.

زي:

```ini
disable_functions=system,exec,passthru,shell_exec
```

---

حتى لو المبرمج كتب:

```php
system(...)
```

مش هتشتغل.

---

وده Layer حماية إضافية.

---

## open_basedir

دي Feature في PHP.

---

بتقول للموقع:

```
أنت مسموحلك تشوف الملفات هنا بس.
```

---

مثلاً.

```
/var/www/html
```

---

لو المهاجم حاول يقرأ:

```
/etc/passwd
```

PHP تمنعه.

---

حتى لو فيه LFI.

---

## Reject Double Encoding

إحنا قبل كده كنا بنعمل:

```
%3B
```

---

وأحيانًا:

```
%253B
```

---

وده Double Encoding.

---

الموقع المفروض يرفضه.

---

## Reject non-ASCII

بعض الناس تستخدم Unicode

علشان تعدي الفلاتر.

---

الموقع يرفضه.

---

## Avoid Outdated Libraries

يعني.

متستخدمش:

PHP CGI القديم.

---

ولا Libraries فيها CVEs.

---

حدث Dependencies باستمرار.

---

## آخر نقطة

الكتاب ختم بحاجة مهمة جدًا.

قال:

حتى لو:

- عملت Validation
    
- عملت Sanitization
    
- فعلت WAF
    
- استخدمت أقل صلاحيات
    
- منعت `system()`
    
- حدثت السيرفر
    

لسه ممكن يبقى فيه Bug.

ليه؟

لأن التطبيقات الكبيرة ممكن تحتوي على **ملايين أسطر من الكود**، وغلطة واحدة في أي مكان قد تفتح بابًا لـ Command Injection أو غيرها من الثغرات.

عشان كده، الحماية الحقيقية بتكون من دمج حاجتين مع بعض:

1. **Secure Coding**: كتابة الكود بطريقة آمنة من البداية (استخدام بدائل لـ `system()`, Validation, Sanitization, أقل صلاحيات...).
    
2. **Penetration Testing**: اختبار التطبيق باستمرار بنفس عقلية المهاجم، لأن الاختبارات العملية هي اللي بتكشف الأخطاء اللي ممكن تكون فاتت أثناء التطوير.
    

وده السبب إن فرق الـ Security في الشركات الكبيرة بتعمل **Code Review + Penetration Testing** معًا، لأن كل واحد فيهم بيكمل التاني.

